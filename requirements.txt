# Full pipeline: scraping, embedding, analysis, and website generation.
#
# This is the human-facing list, for running the whole pipeline locally. The
# website build alone needs far less; see requirements-ci.txt, which is what
# the deploy workflow installs and is pinned exactly.
#
# torch is deliberately left unpinned here. If you have a GPU, install with
# `uv sync` instead: pyproject.toml points torch at the cu126 wheel index,
# which plain pip will not do (see the PyTorch CUDA note in CLAUDE.md).

# Scraping
requests>=2.34.2
beautifulsoup4>=4.15.0
lxml>=6.1.1
tqdm>=4.68.2

# Config
pyyaml>=6.0.3

# Oral/spotlight schedules (analysis/extract_orals.py, analysis/match_orals.py)
pdfplumber>=0.11.0      # word-level PDF extraction with font + coordinates
rapidfuzz>=3.9.0        # fuzzy title matching against the proceedings

# Embeddings
torch
transformers>=4.57.6
adapters>=1.3.0
sentence-transformers>=5.6.0

# Dimensionality reduction + clustering
umap-learn>=0.5.12
pacmap>=0.7.2
scikit-learn>=1.7.2
numpy>=2.2.6

# Visualization
plotly>=6.8.0
networkx>=3.4.2

# Website generation
jinja2>=3.1.6
pillow>=10.0.0         # analysis/logo_assets.py: year logos to WebP
